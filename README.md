# Personal webpage

Source for my personal webpage: https://castamartin.github.io/

It's a single HTML page plus the papers, slides, CV and a photo. To change
something, edit `index.html` and commit — the site updates in about a minute.

Files:
- `index.html` — the page
- `photo_me.jpg` — photo
- `martin-casta-cv.pdf` — CV (source: `martin-casta-cv.tex`)
- `papers/`, `slides/` — the PDFs the page links to
- `sitemap.xml`, `robots.txt` — help search engines index the site
- `.nojekyll` — serve files as-is
- `download_files.py` — one-off helper that pulled the PDFs from Drive
